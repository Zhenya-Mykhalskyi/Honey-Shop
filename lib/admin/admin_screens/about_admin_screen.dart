import 'package:flutter/material.dart';

class AboutAdminScreen extends StatelessWidget {
  const AboutAdminScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Про Адміна'),
      ),
    );
  }
}
